/// @file app.hpp
/// @brief Application entry point interface.

#pragma once

/// @brief Runs the main application loop.
///
/// Configures peripherals and enters an infinite loop that blinks LED0.
/// This function does not return.
void app_run();
